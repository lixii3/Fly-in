[project]
name = "fly_in"
version = "0.1.0"
description = "Managing drones crossing a graph in the smallest amount of turns possible"
authors = [
    {name = "lmongili"}
]
requires-python = ">=3.10"
dependencies = [
    "pydantic (>=2.13.4,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
